{
  "name": "@signalk/set-system-time",
  "version": "1.5.0",
  "description": "Signal K server plugin to set system date & time on Signal K data, usually from a GPS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "signalk-node-server-plugin"
  ],
  "author": "teppo.kurki@iki,fi",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm run build && tsc --allowJs --checkJs --noEmit --module commonjs --target es2019 --types node test/*.test.js",
    "lint": "eslint .",
    "test": "npm run build && node --test test/*.test.js",
    "ci": "npm run lint && npm run typecheck && npm test"
  },
  "files": [
    "dist/**"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.55.0",
    "@typescript-eslint/parser": "^8.55.0",
    "@types/node": "^25.2.3",
    "@eslint/js": "^9.0.0",
    "baconjs": "^3.0.23",
    "eslint": "^9.0.0",
    "globals": "^15.3.0",
    "typescript": "^5.9.3"
  }
}
